spring:
  main:
    banner-mode: off
  mail:
    username: deleted
    password: deleted
    protocol: smtp
    default-encoding: UTF-8
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    test-connection: false

logging:
  file: simpleSpringBootMail.log