1. jstart message brocker (Apache MQ) with "docker-compose up activemq" or install and set up  Apache MQ (http://activemq.apache.org/)
2. run reciver after sender or sender after reciver

http://localhost:8161/admin  -- Apache MQ Monitoring